{
  "name": "amulet-essentials",
  "private": true,
  "description": "browser-side recogniser bank: tools/bank_browser.mjs embeds the reference pictures with the SAME CLIP build the identify page runs, so query and bank never disagree",
  "type": "module",
  "dependencies": { "@xenova/transformers": "2.17.2" }
}
